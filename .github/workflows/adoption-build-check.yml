name: Adoption build verification

on:
  push:
    paths:
      - '.github/workflows/adoption-build-check.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install
      - run: npm run build
      # Re-run after adding the adoption application Suspense boundary.
